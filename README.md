# URL-Shortening-Service
Designed and implemented a URL shortening system in C using a custom hash table with chaining to map long URLs to unique short codes, achieving O(1) average-case lookup and insertion.
